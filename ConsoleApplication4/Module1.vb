Module Module1

    Sub Main()
        Console.WriteLine("hello:")

        Dim abby As Integer = Val(Console.ReadLine())
        Dim a As Integer = 0
        Dim numberarray(abby) As Integer

        Do
            numberarray(a) = Val(Console.ReadLine)
            a = a + 1

        Loop While (a < abby)




    End Sub

End Module

